#include <stdio.h>
#include <stdlib.h>
#include "linkedlist.h"

int main()
{
    /*Sizenode_t* head = malloc(sizeof(node_t));
    head->value = 5;

    node_t* node1 = malloc(sizeof(node_t));
    head->next = node1;
    node1->value = 8;

    node_t* node2 = malloc(sizeof(node_t));
    node1->next = node2;
    node2->value = 6;

    node2->next = NULL;

    print_list(head);

    int result = number_of_nodes(head);
    printf("%d", result);
     This function should take a head as a parameter and
    return the number of node(s) in the list as an integer.*/




    return 0;
}